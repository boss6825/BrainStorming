# Divergent seeds — `{YYYY-MM-DD-slug}`

Judgement **off**. Quantity and mechanism diversity first. Do not score. Do not kill. Do not converge early.

Generated by Opus using the anti-clustering levers in the creative-thinking toolkit. Full templates: [`../../creative-thinking-toolkit/07-multi-model-panel.md`](../../creative-thinking-toolkit/07-multi-model-panel.md).

Do not request hidden chain-of-thought. Each seed is a conclusion plus why it is distinct.

## Diversity setup

Levers in force for this run:

- **Assumption Breaker** — invert or delete a load-bearing assumption from the brief/research.
- **Idea Cascade** — chain from each break into adjacent, weirder variants without returning to the same mechanism.
- **Random Provocation** — inject an unrelated constraint or domain object and force a fit.
- **Quantity-forcing** — at least **25** seeds; **no repeated core mechanism** across the set.

If seeds start clustering around one mechanism, stop and re-seed that cluster until mechanisms diverge.

## Diversity check (required)

- [ ] No repeated core mechanism
- [ ] At least one cross-domain / distant-analogy seed
- [ ] Seeds 16–25 (and beyond) are weirder than 1–15
- [ ] At least one seed the orchestrator would not normally suggest

## Runtime persona assignment

Assigned thinking lenses for the upcoming panel — cognitive-style-inspired, not clinical, not claims about a model or a person. Resolve `{… model id}` from `cursor-agent --list-models`. Do not invent IDs. Distinct personas need per-seat prompt files under `prompts/` (`cursor-panel.sh --seat`).

| Actual model ID | Assigned role | Cognitive-style-inspired lens | Prompt file |
|---|---|---|---|
| `{grok model id}` | Contrarian field investigator | Assumption Breaker + Literalist | `prompts/seat-{label}.md` |
| `{gpt model id}` | Reliability and systems architect | Systemizer + Bottom-Up Builder | `prompts/seat-{label}.md` |
| `{gemini model id}` | Comparative cross-domain researcher | Pattern Transfer + Synesthetic Translation | `prompts/seat-{label}.md` |
| `{composer model id}` | Prototype-obsessed product mechanic | Deep-Tunnel + Random Provocation | `prompts/seat-{label}.md` |
| `{local Codex default}` | Forensic research methodologist | Bottom-Up Builder | plugin turn; provenance header before listing |

## Assumptions cleared

- {assumption cleared}
- {assumption cleared}
- {assumption cleared}

## Seed set

At least **25** seeds. No shared core mechanism.

### S-01 — {name}

- **Mechanism** (must be unique in this list):
- **Why it's distinct:**
- **Question for the panel:**
- **Assigned seats (optional):**

<!-- Repeat S-02 … S-25. S-16–S-25 must be the weirder band. -->

## Mechanism coverage check

| Mechanism family | Seed IDs | Distinct? |
|---|---|---|
| `{mechanism family}` | {n, n, …} | {yes/no} |

If any row fails distinctness, regenerate those seeds before panel fan-out.

## Distinctness shortlist

Not a ranking. Candidates that clearly do not share a core mechanism and are worth panel depth:

- Seed {N} — {one-line distinctness note}

## Shared questions (optional packet)

If used, every member still gets a **distinct persona prompt**. Shared questions are extra, not a substitute for `--seat`.

1. {shared question}

## What the panel must not do

- Do not converge yet.
- Do not majority-vote.
- Do not flatten contradictions into a fake consensus.
- Do not see other members' answers during Mode 2.

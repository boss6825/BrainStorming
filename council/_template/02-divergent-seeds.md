# Divergent seeds — `{YYYY-MM-DD-slug}`

Judgement **off**. Quantity and mechanism diversity first. Do not score. Do not kill. Do not converge early.

## Diversity setup

Levers in force for this run:

- **Assumption Breaker** — invert or delete a load-bearing assumption from the brief/research.
- **Idea Cascade** — chain from each break into adjacent, weirder variants without returning to the same mechanism.
- **Random Provocation** — inject an unrelated constraint or domain object and force a fit.
- **Quantity-forcing** — at least 25 seeds; **no repeated core mechanism** across the set.

If seeds start clustering around one mechanism, stop and re-seed that cluster until mechanisms diverge.

## Runtime persona assignment

Assigned thinking lenses for the upcoming panel — cognitive-style-inspired, not clinical, not claims about a model or a person.

| Actual model ID | Assigned role | Cognitive-style-inspired lens |
|---|---|---|
| `{grok model id}` | Contrarian field investigator | Assumption Breaker + Literalist |
| `{gpt model id}` | Reliability and systems architect | Systemizer + Bottom-Up Builder |
| `{gemini model id}` | Comparative cross-domain researcher | Pattern Transfer + Synesthetic Translation |
| `{composer model id}` | Prototype-obsessed product mechanic | Deep-Tunnel + Random Provocation |
| `codex` | Forensic research methodologist | Bottom-Up Builder |

Resolve `{… model id}` placeholders at session start from `cursor-agent --list-models` (or equivalent). Do not invent IDs.

## Assumptions cleared

Assumptions suspended for seed generation (list explicitly so panel members can reinstate or attack them later):

- {assumption cleared}
- {assumption cleared}
- {assumption cleared}

## Seed set

At least **25** seeds. Seeds **16–25** (and beyond) must be weirder than 1–15. No shared core mechanism.

1. {seed} — mechanism: `{mechanism}`
2. {seed} — mechanism: `{mechanism}`
3. {seed} — mechanism: `{mechanism}`
4. {seed} — mechanism: `{mechanism}`
5. {seed} — mechanism: `{mechanism}`
6. {seed} — mechanism: `{mechanism}`
7. {seed} — mechanism: `{mechanism}`
8. {seed} — mechanism: `{mechanism}`
9. {seed} — mechanism: `{mechanism}`
10. {seed} — mechanism: `{mechanism}`
11. {seed} — mechanism: `{mechanism}`
12. {seed} — mechanism: `{mechanism}`
13. {seed} — mechanism: `{mechanism}`
14. {seed} — mechanism: `{mechanism}`
15. {seed} — mechanism: `{mechanism}`
16. {seed} — mechanism: `{mechanism}` *(weirder band)*
17. {seed} — mechanism: `{mechanism}` *(weirder band)*
18. {seed} — mechanism: `{mechanism}` *(weirder band)*
19. {seed} — mechanism: `{mechanism}` *(weirder band)*
20. {seed} — mechanism: `{mechanism}` *(weirder band)*
21. {seed} — mechanism: `{mechanism}` *(weirder band)*
22. {seed} — mechanism: `{mechanism}` *(weirder band)*
23. {seed} — mechanism: `{mechanism}` *(weirder band)*
24. {seed} — mechanism: `{mechanism}` *(weirder band)*
25. {seed} — mechanism: `{mechanism}` *(weirder band)*

## Mechanism coverage check

| Mechanism family | Seed IDs | Distinct? |
|---|---|---|
| `{mechanism family}` | {n, n, …} | {yes/no} |
| `{mechanism family}` | {n, n, …} | {yes/no} |
| `{mechanism family}` | {n, n, …} | {yes/no} |

If any row fails distinctness, regenerate those seeds before panel fan-out.

## Distinctness shortlist

Not a ranking. Candidates that clearly do not share a core mechanism and are worth panel depth:

- Seed {N} — {one-line distinctness note}
- Seed {N} — {one-line distinctness note}
- Seed {N} — {one-line distinctness note}
- Seed {N} — {one-line distinctness note}
- Seed {N} — {one-line distinctness note}

## Questions handed to every panel member

Same packet for all models. No member-specific framing beyond their assigned role/lens.

1. {shared question}
2. {shared question}
3. {shared question}
4. {shared question}
5. {shared question}
